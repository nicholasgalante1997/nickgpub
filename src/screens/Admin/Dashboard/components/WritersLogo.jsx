import React from 'react';
import Logo from '../../../../components/common/Logo';
import '../styles/dashboard.scss';

const WritersLogo = () => (
  <div className="logo-container">
    <Logo size={54} className="logo" />
  </div>
);

export default WritersLogo;
